services:
  api:
    build: .
    image: fastapi-py:latest
    container_name: fastapi-py
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    restart: unless-stopped