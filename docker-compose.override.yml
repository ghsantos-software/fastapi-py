# O "docker compose" aplica este arquivo automaticamente em cima do base.
# Só conveniências de desenvolvimento local.
services:
  api:
    volumes:
      - .:/app
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload